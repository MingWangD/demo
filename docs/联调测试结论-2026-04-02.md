# 联调测试结论（2026-04-02）

## 1. 本次在当前容器环境已执行项
1. 前端生产构建：`cd vue && npm run build`（通过）。
2. 后端打包验证：`cd springboot && mvn -s mvn-settings.xml -q -DskipTests package`（未通过，受仓库网络解析限制）。
3. 本地 MySQL 连通性检查：容器内未安装 mysql client，无法直接执行 SQL 连通性命令。

## 2. 结论
- **前端代码可构建，页面静态资源可产出。**
- **后端在当前容器中无法完成依赖解析，暂不能确认“后端服务可启动 + 接口可联调”。**
- 因此“功能是否都能跑通”的结论是：**前端构建链路跑通，后端运行链路在当前环境未被完整验证**。

## 3. 阻塞原因
- Maven 依赖父 POM `org.springframework.boot:spring-boot-starter-parent:3.3.1` 无法下载：
  - 使用中央仓库时出现 403；
  - 使用阿里云镜像时 DNS 解析失败（`maven.aliyun.com` 不可达）。

## 4. 建议你本机做一次“5分钟终验”
1. 在本机初始化数据库：
   - `source springboot/src/main/resources/sql/schema.sql`
   - `source springboot/src/main/resources/sql/data.sql`
2. 启动后端：`cd springboot && mvn spring-boot:run`
3. 启动前端：`cd vue && npm run dev`
4. 用默认账号完成最短链路：
   - `t_zhang/123456` 登录 -> 教师高风险筛选；
   - `s_wang/123456` 登录 -> 学生总览/风险趋势 -> 作业提交 -> 考试资格查看。

## 5. 你接下来的节奏建议
- 现在可以先开做前端界面优化（布局、颜色体系、动效和空状态美化）。
- 同时并行准备论文：系统架构图、ER 图、算法流程图、对比实验与局限性章节。

## 6. 2026-04-02 复测结果（你反馈“已重新配置 Maven”后）
1. 复测命令：`cd springboot && mvn -s mvn-settings.xml -q -DskipTests package`
2. 结果：仍失败，错误由“Unknown host”变为“Network is unreachable”（访问 `https://maven.aliyun.com/repository/public`）。
3. 结论：当前容器外网到 Maven 仓库仍不可用，导致后端依赖无法下载，无法进入服务启动与接口联调阶段。
4. 可确认项：前端 `cd vue && npm run build` 仍可稳定通过。
