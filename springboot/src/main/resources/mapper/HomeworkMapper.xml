<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.HomeworkMapper">

    <select id="selectAll" resultType="com.example.entity.Homework">
        select * from `homework`
        <where>
            <if test="id != null"> and id = #{id}</if>
        </where>
        order by id desc
    </select>

    <select id="selectById" resultType="com.example.entity.Homework">
        select * from `homework` where id = #{id}
    </select>

    <delete id="deleteById">
        delete from `homework` where id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.Homework" useGeneratedKeys="true" keyProperty="id">
        insert into `homework`(course_id, teacher_id, title, content, deadline, total_score, create_time, update_time)
        values (#{courseId}, #{teacherId}, #{title}, #{content}, #{deadline}, #{totalScore}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById" parameterType="com.example.entity.Homework">
        update `homework`
        <set>
            <if test="courseId != null">course_id = #{courseId},</if>
            <if test="teacherId != null">teacher_id = #{teacherId},</if>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="deadline != null">deadline = #{deadline},</if>
            <if test="totalScore != null">total_score = #{totalScore},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

</mapper>