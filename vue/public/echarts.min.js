/* Local fallback stub for offline defense demo.
 * Replace with official ECharts dist file when network-restricted environment requires full charts offline.
 */
window.echarts = window.echarts || {
  init: function () {
    return {
      setOption: function () {}
    }
  }
};
